import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juzamin.recital',
  appName: 'القرآن - الشيخ محمد شريف',
  webDir: 'dist'
};

export default config;
